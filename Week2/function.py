# python 함수
def get_vat(price, vat_rate=0.1):
  return price * vat_rate

get_vat(10000)
get_vat(20000, 0.3)

# 메인 함수를 호출
def main():
  # 블라블라
  
if __name__ == "__main__":
  main()  # 함수 이름은 임의로 설정 가능

# 분기에서 결과가 나오는 부분을 먼저 삼항연산자로 처리해서 코드 줄 수를 줄일수 있을것
# chatGPT 사용해서 코드 작성해보기

